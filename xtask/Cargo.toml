[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false
description = "Dev tooling: `cargo xtask features` computes the cargo features a deployment needs."

# Deliberately no dependencies. This parses an env file and greps source for
# a handful of markers; pulling a parser crate in for that would make the
# tool that trims the dependency tree a growth on it.
[dependencies]
