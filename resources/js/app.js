// The application entry — `@vite('resources/js/app.js')` in the layout.
//
// Deliberately almost nothing: the sample's pages are server-rendered, and
// this exists to prove the pipeline (hot reload under `npm run dev`, a
// content-hashed bundle under `npm run build`) rather than to be a frontend.

console.debug("rainier-sample: assets loaded");
